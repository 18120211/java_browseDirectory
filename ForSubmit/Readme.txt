1. Link youtube:

2. Lưu ý:
	2.1. Để sử dụng chương trình tốt nhất thì sử dụng Terminal được hỗ trợ bởi Java, tránh sử dụng cmd mặc định của window
		sẽ bị lỗi ở khả năng thể hiện(xóa màn hình)
	2.2. Những tập tin dữ liệu `slang.txt`, `editedSlang.txt`, `history.dat` cần được đặt cùng thư mục với tập tin thực thi
		'BrowseDirectory.jar'
	2.3. Cách chạy chương trình
		2.3.1. Chạy bằng lệnh - `cd ./ForSubmit/release
					 java -jar ./BrowseDirectory.jar`
 		2.3.2. Chạy trực tiếp bằng file - Chạy tập tin `run.bat` trong thư mục ./ForSubmit/release